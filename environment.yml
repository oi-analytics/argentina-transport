# Conda environment specification
# To import/create a copy of the environment:
#    conda env create -f .environment.yml
name: argentina-transport
channels:
  - conda-forge
  - defaults
dependencies:
  - boltons
  - cartopy
  - colour
  - dask
  - fiona
  - GDAL
  - geopandas==0.4.0
  - geopy
  - ipopt
  - matplotlib
  - networkx
  - numpy
  - openpyxl
  - pandas
  - pylint  # dev
  - pyomo
  - pytest  # test
  - pytest-cov  # test
  - rasterio
  - rasterstats
  - requests
  - rtree
  - SALib
  - scipy
  - shapely
  - sphinx  # docs
  - tqdm
  - xlrd
  - pip:
    - snkit
